[project]
name = "metaswarm"
version = "0.1.0"
description = "Self-evolving multi-agent system — agents that improve each other"
requires-python = ">=3.11"
dependencies = [
    "autogen-agentchat>=0.7.0",
    "autogen-ext[openai]>=0.7.0",
    "azure-cosmos>=4.7.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "fastapi>=0.100.0",
    "uvicorn>=0.20.0",
]

[project.scripts]
metaswarm = "core.orchestrator:main"
